<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ToDos" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.ToDos.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.ToDos</item>
    </style>
</resources>